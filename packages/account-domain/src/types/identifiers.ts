// Canonical identifiers used across the Account → Workspace → Module → Entity hierarchy.
export type AccountId = string;
export type WorkspaceId = string;
export type ModuleKey = string;
export type EntityId = string;
