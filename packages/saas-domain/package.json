{
  "name": "@ng-events/saas-domain",
  "version": "1.0.0",
  "description": "SaaS business domain models - Pure TypeScript with core-engine (frontend & backend safe, no firebase-admin)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint . --ext .ts"
  },
  "keywords": [
    "ddd",
    "domain",
    "business-logic",
    "saas"
  ],
  "author": "ng-events team",
  "license": "MIT",
  "dependencies": {
    "@ng-events/account-domain": "*",
    "@ng-events/core-engine": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "tsconfig": {
    "compilerOptions": {
      "baseUrl": ".",
      "paths": {
        "firebase-admin": ["__forbidden__"],
        "@ng-events/platform-adapters/*": ["__forbidden__"]
      }
    }
  },
  "copilot": {
    "notes": [
      "saas-domain is pure domain model (frontend & backend safe)",
      "Never import firebase-admin here",
      "Do not import platform-adapters server helpers",
      "Only use core-engine or other safe TypeScript packages"
    ]
  }
}
