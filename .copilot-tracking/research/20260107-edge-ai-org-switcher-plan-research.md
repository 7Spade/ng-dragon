<!-- markdownlint-disable-file -->

# Task Research Notes: Org switcher plan via edge-ai collection

## Research Executed

### File Analysis

- .github/collections/edge-ai-tasks.md
  - Describes edge-ai task workflow: Task Researcher → Task Planner → Task Plan Implementation; shows how plans, details, and prompts are generated under `.copilot-tracking/` and how to configure VS Code instruction/prompt locations.
- .copilot-tracking/research/20260107-org-switcher-architecture-research.md
  - Existing org switcher research: recommends workspace/session facade, projection-fed org lists, permission cache gating for team creation, partner creation with org context.
- .copilot-tracking/plans/20260107-mermaid-file-tree-plan.instructions.md
  - Example plan structure generated by task-planner (phases/tasks checklist); confirms expected output locations for plan/instructions.
- docs/Mermaid-架構層.md & docs/Mermaid-實作指引.md
  - Reinforce layer boundaries, workspace gating, and UI consumption of projections—must be respected in any new plan for org switcher refactor.
- packages/ui-angular/src/app/core/auth/firebase-auth-bridge.service.ts
  - Shows existing integration of @delon/auth token sync; indicates precedent for wrapping auth in a bridge service.
- packages/ui-angular/package.json
  - Declares @delon/acl dependency, confirming ACL integration target.

### Code Search Results

- `ls .copilot-tracking/plans` → only mermaid file tree plan exists; no plan yet for org switcher.
- `grep -R "acl" packages/ui-angular/src/app | head` (suggested next) to locate current ACL usage patterns.
- Header widget target: `packages/ui-angular/src/app/layout/basic/widgets/user.component.ts` (hardcoded org lists, inline checks).

### External Research

- #githubRepo:"(not used)"  
  - No external repositories consulted; focus is on internal edge-ai workflow doc.
- #fetch:(none)  
  - No remote HTTP resources required.

### Project Conventions

- Standards referenced: edge-ai Task Plan Implementation instructions, Task Planner/Task Researcher agent usage, architecture and implementation guides (workspace/module gating, UI boundaries).
- UI permissions must be enforced via @delon/acl with computed roles/abilities from a permission cache (docs/Mermaid-實作指引.md) rather than inline checks.
- Instructions followed: Research-only scope; prepare iterative plan generation using task-planner with existing org switcher research as input.

## Key Discoveries

### Project Structure

- Plans live in `.copilot-tracking/plans/*.instructions.md`; detailed steps in `.copilot-tracking/details/*.md`; prompts in `.copilot-tracking/prompts/implement-*.prompt.md`.
- Research inputs are stored under `.copilot-tracking/research/` and can be referenced by task-planner via `#file:` directive.
- Permissions stack: @delon/auth bridge already present; @delon/acl available for applying computed abilities when switching workspace.

### Implementation Patterns

- edge-ai collection prescribes iterative flow: enrich research → run task-planner to synthesize phased checklist → use generated prompt to apply task-implementation instructions.
- Settings must include `.copilot-tracking/plans` and `.copilot-tracking/prompts` in `chat.instructionsFilesLocations` / `chat.promptFilesLocations` to surface generated assets.
- Org switcher work should be planned as phases (e.g., session facade design, projection wiring, ACL permission cache integration, UI refactor to presentation-only).
- Permission enforcement should mirror the pattern in docs/Mermaid-實作指引.md: fetch permissions for selected workspace, cache, then set ACL roles/abilities via ACLService; header widget should consume facade signals without business logic.

### Complete Examples

```markdown
---
mode: task-planner
title: Plan org switcher architecture alignment
---
#file: .copilot-tracking/research/20260107-org-switcher-architecture-research.md
#file: .copilot-tracking/research/20260107-edge-ai-org-switcher-plan-research.md
Create a phased plan to replace hardcoded org dropdown data with projection-driven workspace/session facade, enforce permission cache with @delon/acl for team creation gating, and keep header-user component presentation-only.
```

```typescript
// ACL application sketch to inform plan tasks
await this.permissionApi.switchWorkspace(workspaceId);
this.acl.setFull({
  role: permissions.computed,
  ability: Object.keys(permissions.computed).filter(k => permissions.computed[k])
});
```

### API and Schema Documentation

- Not applicable for this research; focus is on planning workflow, ACL integration pattern, and architecture boundaries already documented in org switcher research and Mermaid guides.

### Configuration Examples

```markdown
---
mode: agent
title: Implement org switcher architecture alignment
---
/implement-org-switcher-alignment phaseStop=true taskStop=true
```

- The generated implement prompt should target the new plan name (to be produced by task-planner) and can use `phaseStop`/`taskStop` for iterative control per edge-ai collection.

### Technical Requirements

- Plan must produce phased checklist and detailed tasks adhering to architecture docs (workspace/module gating, UI uses projections, no domain logic in Angular components).
- Include ACL integration tasks: apply permission cache to ACLService on workspace switch; guard team creation ability via ACL roles/abilities instead of inline checks.
- Task-planner should reference both org switcher research files to ground phases in prior findings and edge-ai workflow guidance.
- Implementation will later rely on ACL/permission cache patterns noted in docs/Mermaid-實作指引.md and the existing @delon/auth bridge.

## Recommended Approach

Use the edge-ai task-planner agent to generate a new plan for the org switcher refactor by feeding both org switcher research files. Ensure the plan outputs:
1) `.copilot-tracking/plans/*org-switcher*-plan.instructions.md` with phases (facade design, projection wiring, ACL permission cache integration, UI refactor, validation),
2) matching details file enumerating tasks per phase, and
3) implement prompt under `.copilot-tracking/prompts/implement-*.prompt.md`. Configure VS Code settings so these assets are discoverable, then iterate with `phaseStop`/`taskStop` flags as needed.

## Implementation Guidance

- **Objectives**: Produce a clear, phased plan for org switcher alignment using edge-ai workflow; keep alignment with architecture boundaries, ACL-driven permission enforcement, and prior org switcher research.
- **Key Tasks**: Run task-planner with both research files; ensure phases include ACL permission cache application; review/edit generated plan and details; generate implement prompt; update settings.json entries if missing to surface plan/prompt.
- **Dependencies**: Existing org switcher research files; edge-ai task collection; @delon/auth bridge; @delon/acl for permission enforcement; architecture/implementation docs for workspace gating and UI boundaries.
- **Success Criteria**: New plan/details/prompt files generated for org switcher work; plan reflects facade/projection/ACL permission gating phases; assets discoverable via VS Code configuration; ready for iterative execution with task-implementation instructions.
