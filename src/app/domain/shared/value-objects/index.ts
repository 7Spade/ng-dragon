/**
 * Shared Value Objects
 * 
 * 共享的值物件,可在所有領域模型中使用
 */

export { Id, UuidId, StringId, NumericId } from './id.value-object';
export { Email } from './email.value-object';
export { Slug } from './slug.value-object';
export { Timestamp } from './timestamp.value-object';
