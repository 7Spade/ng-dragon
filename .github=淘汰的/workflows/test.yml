name: Test Placeholder

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Add targeted tests per change; use MCP servers for setup (context7, playwright)."
