# Copilot Instructions

## CI Constraints
- CI environment cannot access Firebase Auth
- All E2E tests in CI must mock authentication
- Do not generate real login flows for CI

## Testing Strategy
- Real Firebase login: local / staging only
- CI: mock user state or skip auth-required tests
