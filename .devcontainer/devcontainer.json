{
  "name": "MCP DevContainer",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "git": "latest",
    "node": "lts"
  },
  "remoteUser": "vscode",
  "workspaceFolder": "/workspaces/ng-dragon",
  "postCreateCommand": [
    // 先啟動 MCP Docker 容器 (後台跑，不阻塞)
    "docker run -d --name awesome-copilot --restart unless-stopped ghcr.io/microsoft/mcp-dotnet-samples/awesome-copilot:latest",
    
    // 安裝 MCP 核心服務
    "npx -y @modelcontextprotocol/server-filesystem ./",
    "npx -y @modelcontextprotocol/server-memory",
    "npx -y @modelcontextprotocol/server-everything",
    "npx -y @modelcontextprotocol/server-sequential-thinking",
    "npx -y github:NightTrek/Software-planning-mcp",
    
    // 安裝 Playwright MCP Server
    "npx -y @executeautomation/playwright-mcp-server",
    
    // Codacy MCP
    "export CODACY_ACCOUNT_TOKEN=yHFBU9bPJjpAOK60iHS0",
    "export CODACY_CLI_VERSION=latest",
    "npx -y @codacy/codacy-mcp",
    
    // 最後安裝專案依賴
    "cd /workspaces/ng-dragon && yarn install --immutable"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "github.copilot-chat",
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint",
        "stylelint.vscode-stylelint",
        "cipchk.ng-alain-extension-pack",
        "ms-vscode.vscode-typescript-next"
      ]
    }
  }
}
