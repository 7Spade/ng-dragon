# 階段 1️⃣: 基礎架構設置

## 🎯 目標

建立切換器功能所需的基礎架構,包括:
- 類型定義 (TypeScript interfaces)
- Store 架構 (NgRx Signals)
- 服務層 (Firebase 整合)
- 共用工具

**預估時間**: 2-3 小時

---

## 📁 檔案結構

```
src/
├── app/
│   ├── domain/                           # Domain Layer (領域層)
│   │   ├── account/
│   │   │   └── entities/
│   │   │       └── account.entity.ts     # 身份實體定義
│   │   └── workspace/
│   │       └── entities/
│   │           └── workspace.entity.ts   # 工作區實體定義
│   │
│   ├── application/                      # Application Layer (應用層)
│   │   └── store/
│   │       ├── account/
│   │       │   └── account.store.ts      # 身份 Store
│   │       └── workspace/
│   │           └── workspace.store.ts    # 工作區 Store
│   │
│   ├── infrastructure/                   # Infrastructure Layer (基礎設施層)
│   │   ├── account/
│   │   │   └── services/
│   │   │       └── account.service.ts    # 身份服務實作
│   │   └── workspace/
│   │       └── services/
│   │           └── workspace.service.ts  # 工作區服務實作
│   │
│   └── shared/                           # Shared Layer (共享層)
│       └── constants/
│           └── breakpoints.ts            # 響應式斷點定義
```

---

## 📝 步驟 1: 建立領域實體定義

### 1.1 身份實體 (`account.entity.ts`)

```typescript
// src/app/domain/account/entities/account.entity.ts

/**
 * 帳號類型枚舉
 */
export enum AccountType {
  User = 'user',
  Organization = 'organization',
  Team = 'team',
  Partner = 'partner'
}

/**
 * 帳號角色枚舉
 */
export enum AccountRole {
  Owner = 'owner',
  Admin = 'admin',
  Member = 'member',
  Collaborator = 'collaborator',
  Viewer = 'viewer'
}

/**
 * 基礎帳號介面
 */
export interface BaseAccount {
  id: string;
  type: AccountType;
  name: string;
  avatar?: string;
  createdAt: Date;
  updatedAt: Date;
}

/**
 * 使用者帳號
 */
export interface UserAccount extends BaseAccount {
  type: AccountType.User;
  email: string;
  photoURL?: string;
  displayName: string;
}

/**
 * 組織帳號
 */
export interface OrganizationAccount extends BaseAccount {
  type: AccountType.Organization;
  orgLogo?: string;
  domain?: string;
  memberCount: number;
}

/**
 * 團隊帳號
 */
export interface TeamAccount extends BaseAccount {
  type: AccountType.Team;
  organizationId: string;
  memberCount: number;
}

/**
 * 合作夥伴帳號
 */
export interface PartnerAccount extends BaseAccount {
  type: AccountType.Partner;
  companyName: string;
  partnerLevel: 'bronze' | 'silver' | 'gold' | 'platinum';
}

/**
 * 聯合帳號類型
 */
export type Account = UserAccount | OrganizationAccount | TeamAccount | PartnerAccount;

/**
 * 帳號成員關係
 */
export interface AccountMembership {
  accountId: string;
  userId: string;
  role: AccountRole;
  joinedAt: Date;
}

/**
 * 當前帳號狀態
 */
export interface CurrentAccountState {
  account: Account | null;
  membership?: AccountMembership;
  isLoading: boolean;
  error: string | null;
}
```

### 1.2 工作區實體 (`workspace.entity.ts`)

```typescript
// src/app/domain/workspace/entities/workspace.entity.ts

/**
 * 工作區類型枚舉
 */
export enum WorkspaceType {
  Project = 'project',
  Department = 'department',
  Client = 'client',
  Campaign = 'campaign',
  Product = 'product',
  Internal = 'internal'
}

/**
 * 工作區狀態枚舉
 */
export enum WorkspaceStatus {
  Active = 'active',
  Archived = 'archived',
  Deleted = 'deleted'
}

/**
 * 工作區介面
 */
export interface Workspace {
  id: string;
  name: string;
  description?: string;
  type: WorkspaceType;
  status: WorkspaceStatus;
  
  // 視覺設計
  avatar?: string;
  color?: string;
  icon?: string;
  
  // 所有權
  ownerId: string;
  accountId: string; // 所屬帳號 ID
  
  // 成員統計
  memberCount: number;
  
  // 時間戳記
  createdAt: Date;
  updatedAt: Date;
  lastAccessedAt?: Date;
  archivedAt?: Date;
}

/**
 * 工作區成員
 */
export interface WorkspaceMember {
  workspaceId: string;
  userId: string;
  role: 'owner' | 'admin' | 'member' | 'viewer';
  joinedAt: Date;
}

/**
 * 工作區偏好設定
 */
export interface WorkspacePreferences {
  userId: string;
  workspaceId: string;
  isFavorite: boolean;
  lastAccessed: Date;
  viewOrder?: number;
}

/**
 * 工作區分組
 */
export interface WorkspaceGroup {
  title: string;
  workspaces: Workspace[];
}

/**
 * 工作區列表狀態
 */
export interface WorkspaceListState {
  workspaces: Workspace[];
  favorites: string[]; // workspace IDs
  recents: string[]; // workspace IDs (最近使用)
  currentWorkspaceId: string | null;
  searchQuery: string;
  isLoading: boolean;
  error: string | null;
}
```

### 1.3 匯出檔案 (`index.ts`)

```typescript
// src/app/domain/account/entities/index.ts
export * from './account.entity';

// src/app/domain/workspace/entities/index.ts
export * from './workspace.entity';
```

---

## 📝 步驟 2: 建立 Application Layer Store

### 2.1 身份 Store (`account.store.ts`)

```typescript
// src/app/application/store/account/account.store.ts
import { computed, inject } from '@angular/core';
import { signalStore, withState, withComputed, withMethods, patchState } from '@ngrx/signals';
import { rxMethod } from '@ngrx/signals/rxjs-interop';
import { pipe, switchMap, tap, catchError, of } from 'rxjs';

import { AccountService } from '@/infrastructure/account/services/account.service';
import { Account, AccountType } from '@/domain/account/entities';

/**
 * 帳號狀態介面
 */
interface AccountState {
  account: Account | null;
  accounts: Account[];  // 所有可用帳號
  isLoading: boolean;
  error: string | null;
}

/**
 * 初始狀態
 */
const initialState: AccountState = {
  account: null,
  accounts: [],
  isLoading: false,
  error: null
};

/**
 * 帳號 Store - 管理當前身份狀態
 */
export const AccountStore = signalStore(
  { providedIn: 'root' },
  
  withState(initialState),
  
  withComputed((store) => ({
    /**
     * 是否已登入
     */
    isAuthenticated: computed(() => !!store.account()),
    
    /**
     * 當前帳號名稱
     */
    accountName: computed(() => {
      const account = store.account();
      return account?.name || '';
    }),
    
    /**
     * 當前帳號頭像
     */
    accountAvatar: computed(() => {
      const account = store.account();
      if (!account) return undefined;
      
      if ('photoURL' in account) return account.photoURL;
      if ('orgLogo' in account) return account.orgLogo;
      return account.avatar;
    }),
    
    /**
     * 按類型分組的帳號
     */
    groupedAccounts: computed(() => {
      const accounts = store.accounts();
      
      return {
        user: accounts.filter(a => a.type === AccountType.User),
        organizations: accounts.filter(a => a.type === AccountType.Organization),
        teams: accounts.filter(a => a.type === AccountType.Team),
        partners: accounts.filter(a => a.type === AccountType.Partner)
      };
    })
  })),
  
  withMethods((store) => {
    const accountService = inject(AccountService);
    
    return {
      /**
       * 載入當前帳號
       */
      loadCurrentAccount: rxMethod<string>(
        pipe(
          tap(() => patchState(store, { isLoading: true, error: null })),
          switchMap((accountId) =>
            accountService.getAccount(accountId).pipe(
              tap((account) => {
                patchState(store, {
                  account,
                  isLoading: false,
                  error: null
                });
              }),
              catchError((error) => {
                patchState(store, {
                  isLoading: false,
                  error: error.message
                });
                return of(null);
              })
            )
          )
        )
      ),
      
      /**
       * 載入所有可用帳號
       */
      loadAvailableAccounts: rxMethod<string>(
        pipe(
          switchMap((userId) =>
            accountService.getUserAccounts(userId).pipe(
              tap((accounts) => {
                patchState(store, { accounts });
              }),
              catchError((error) => {
                console.error('載入帳號列表失敗:', error);
                return of([]);
              })
            )
          )
        )
      ),
      
      /**
       * 切換帳號
       */
      switchAccount: rxMethod<string>(
        pipe(
          tap(() => patchState(store, { isLoading: true, error: null })),
          switchMap((accountId) =>
            accountService.switchAccount(accountId).pipe(
              tap((account) => {
                patchState(store, {
                  account,
                  isLoading: false,
                  error: null
                });
              }),
              catchError((error) => {
                patchState(store, {
                  isLoading: false,
                  error: error.message
                });
                return of(null);
              })
            )
          )
        )
      ),
      
      /**
       * 重置狀態
       */
      reset: () => {
        patchState(store, initialState);
      }
    };
  })
);
```

### 2.2 工作區 Store (`workspace.store.ts`)

```typescript
// src/app/application/store/workspace/workspace.store.ts
import { computed, inject } from '@angular/core';
import { signalStore, withState, withComputed, withMethods, patchState } from '@ngrx/signals';
import { rxMethod } from '@ngrx/signals/rxjs-interop';
import { pipe, switchMap, tap, catchError, of } from 'rxjs';

import { WorkspaceService } from '@/infrastructure/workspace/services/workspace.service';
import { Workspace, WorkspaceStatus } from '@/domain/workspace/entities';

/**
 * 工作區分組介面
 */
export interface WorkspaceGroup {
  title: string;
  workspaces: Workspace[];
}

/**
 * 工作區狀態介面
 */
interface WorkspaceState {
  workspaces: Workspace[];
  favorites: string[]; // workspace IDs
  recents: string[]; // workspace IDs (最近使用)
  currentWorkspaceId: string | null;
  searchQuery: string;
  isLoading: boolean;
  error: string | null;
}

/**
 * 初始狀態
 */
const initialState: WorkspaceState = {
  workspaces: [],
  favorites: [],
  recents: [],
  currentWorkspaceId: null,
  searchQuery: '',
  isLoading: false,
  error: null
};

/**
 * 工作區 Store
 */
export const WorkspaceStore = signalStore(
  { providedIn: 'root' },
  
  withState(initialState),
  
  withComputed((store) => ({
    /**
     * 當前工作區
     */
    currentWorkspace: computed(() => {
      const workspaces = store.workspaces();
      const currentId = store.currentWorkspaceId();
      return workspaces.find(w => w.id === currentId) || null;
    }),
    
    /**
     * 已篩選的工作區列表
     */
    filteredWorkspaces: computed(() => {
      const workspaces = store.workspaces();
      const query = store.searchQuery().toLowerCase();
      
      if (!query) return workspaces;
      
      return workspaces.filter(w =>
        w.name.toLowerCase().includes(query) ||
        w.description?.toLowerCase().includes(query)
      );
    }),
    
    /**
     * 最近使用的工作區
     */
    recentWorkspaces: computed(() => {
      const workspaces = store.workspaces();
      const recentIds = store.recents();
      
      return recentIds
        .map(id => workspaces.find(w => w.id === id))
        .filter((w): w is Workspace => !!w)
        .slice(0, 5); // 只顯示最近 5 個
    }),
    
    /**
     * 我的最愛工作區
     */
    favoriteWorkspaces: computed(() => {
      const workspaces = store.workspaces();
      const favoriteIds = store.favorites();
      
      return workspaces.filter(w => favoriteIds.includes(w.id));
    }),
    
    /**
     * 已封存的工作區
     */
    archivedWorkspaces: computed(() => {
      return store.workspaces().filter(w => w.status === WorkspaceStatus.Archived);
    }),
    
    /**
     * 活躍的工作區
     */
    activeWorkspaces: computed(() => {
      return store.workspaces().filter(w => w.status === WorkspaceStatus.Active);
    }),
    
    /**
     * 分組的工作區
     */
    groupedWorkspaces: computed((): WorkspaceGroup[] => {
      const filtered = store.filteredWorkspaces();
      const favorites = store.favorites();
      const recents = store.recents();
      
      const groups: WorkspaceGroup[] = [];
      
      // 最近使用
      const recentWorkspaces = recents
        .map(id => filtered.find(w => w.id === id))
        .filter((w): w is Workspace => !!w && w.status === WorkspaceStatus.Active)
        .slice(0, 5);
      
      if (recentWorkspaces.length > 0) {
        groups.push({
          title: '最近使用',
          workspaces: recentWorkspaces
        });
      }
      
      // 我的最愛
      const favWorkspaces = filtered.filter(w =>
        favorites.includes(w.id) && w.status === WorkspaceStatus.Active
      );
      
      if (favWorkspaces.length > 0) {
        groups.push({
          title: '我的最愛',
          workspaces: favWorkspaces
        });
      }
      
      // 我的工作區 (owner)
      const myWorkspaces = filtered.filter(w =>
        w.status === WorkspaceStatus.Active &&
        !favorites.includes(w.id) &&
        !recents.slice(0, 5).includes(w.id)
      );
      
      if (myWorkspaces.length > 0) {
        groups.push({
          title: '我的工作區',
          workspaces: myWorkspaces
        });
      }
      
      return groups;
    })
  })),
  
  withMethods((store) => {
    const workspaceService = inject(WorkspaceService);
    
    return {
      /**
       * 載入工作區列表
       */
      loadWorkspaces: rxMethod<string>(
        pipe(
          tap(() => patchState(store, { isLoading: true, error: null })),
          switchMap((accountId) =>
            workspaceService.getWorkspacesByAccount(accountId).pipe(
              tap((workspaces) => {
                patchState(store, {
                  workspaces,
                  isLoading: false,
                  error: null
                });
              }),
              catchError((error) => {
                patchState(store, {
                  isLoading: false,
                  error: error.message
                });
                return of([]);
              })
            )
          )
        )
      ),
      
      /**
       * 設置當前工作區
       */
      setCurrentWorkspace: (workspaceId: string) => {
        patchState(store, { currentWorkspaceId: workspaceId });
        
        // 更新最近使用列表
        const recents = store.recents();
        const newRecents = [
          workspaceId,
          ...recents.filter(id => id !== workspaceId)
        ].slice(0, 10); // 保留最近 10 個
        
        patchState(store, { recents: newRecents });
      },
      
      /**
       * 設置搜尋關鍵字
       */
      setSearchQuery: (query: string) => {
        patchState(store, { searchQuery: query });
      },
      
      /**
       * 切換我的最愛
       */
      toggleFavorite: (workspaceId: string) => {
        const favorites = store.favorites();
        const newFavorites = favorites.includes(workspaceId)
          ? favorites.filter(id => id !== workspaceId)
          : [...favorites, workspaceId];
        
        patchState(store, { favorites: newFavorites });
      },
      
      /**
       * 重置狀態
       */
      reset: () => {
        patchState(store, initialState);
      }
    };
  })
);
```

### 2.3 匯出檔案 (`index.ts`)

```typescript
// src/app/application/store/account/index.ts
export * from './account.store';

// src/app/application/store/workspace/index.ts
export * from './workspace.store';
```

---

## 📝 步驟 3: 建立 Infrastructure Layer 服務實作

### 3.1 身份服務實作 (`account.service.ts`)

```typescript
// src/app/infrastructure/account/services/account.service.ts
import { Injectable, inject } from '@angular/core';
import { Firestore, collection, doc, getDoc, getDocs, query, where } from '@angular/fire/firestore';
import { Observable, from, map, switchMap } from 'rxjs';

import { Account, AccountType } from '@/domain/account/entities';

@Injectable({
  providedIn: 'root'
})
export class AccountService {
  private firestore = inject(Firestore);
  
  /**
   * 取得單一帳號
   */
  getAccount(accountId: string): Observable<Account> {
    const accountRef = doc(this.firestore, 'accounts', accountId);
    
    return from(getDoc(accountRef)).pipe(
      map((snapshot) => {
        if (!snapshot.exists()) {
          throw new Error('Account not found');
        }
        
        return {
          id: snapshot.id,
          ...snapshot.data()
        } as Account;
      })
    );
  }
  
  /**
   * 取得使用者的所有帳號
   */
  getUserAccounts(userId: string): Observable<Account[]> {
    const membershipsRef = collection(this.firestore, 'accountMemberships');
    const q = query(membershipsRef, where('userId', '==', userId));
    
    return from(getDocs(q)).pipe(
      switchMap((snapshot) => {
        const accountIds = snapshot.docs.map(doc => doc.data()['accountId'] as string);
        
        // 並行取得所有帳號資料
        return from(Promise.all(
          accountIds.map(id => getDoc(doc(this.firestore, 'accounts', id)))
        ));
      }),
      map((snapshots) => {
        return snapshots
          .filter(snapshot => snapshot.exists())
          .map(snapshot => ({
            id: snapshot.id,
            ...snapshot.data()
          } as Account));
      })
    );
  }
  
  /**
   * 切換帳號
   */
  switchAccount(accountId: string): Observable<Account> {
    // 儲存到 LocalStorage
    localStorage.setItem('currentAccountId', accountId);
    
    return this.getAccount(accountId);
  }
  
  /**
   * 取得當前帳號 ID
   */
  getCurrentAccountId(): string | null {
    return localStorage.getItem('currentAccountId');
  }
}
```

### 3.2 工作區服務實作 (`workspace.service.ts`)

```typescript
// src/app/infrastructure/workspace/services/workspace.service.ts
import { Injectable, inject } from '@angular/core';
import { Firestore, collection, query, where, getDocs, orderBy } from '@angular/fire/firestore';
import { Observable, from, map } from 'rxjs';

import { Workspace } from '@/domain/workspace/entities';

@Injectable({
  providedIn: 'root'
})
export class WorkspaceService {
  private firestore = inject(Firestore);
  
  /**
   * 取得帳號的所有工作區
   */
  getWorkspacesByAccount(accountId: string): Observable<Workspace[]> {
    const workspacesRef = collection(this.firestore, 'workspaces');
    const q = query(
      workspacesRef,
      where('accountId', '==', accountId),
      orderBy('lastAccessedAt', 'desc')
    );
    
    return from(getDocs(q)).pipe(
      map((snapshot) => {
        return snapshot.docs.map(doc => ({
          id: doc.id,
          ...doc.data()
        } as Workspace));
      })
    );
  }
  
  /**
   * 更新最後存取時間
   */
  updateLastAccessed(workspaceId: string): Observable<void> {
    // 實作更新邏輯
    return from(Promise.resolve());
  }
}
```

### 3.3 匯出檔案 (`index.ts`)

```typescript
// src/app/infrastructure/account/services/index.ts
export * from './account.service';

// src/app/infrastructure/workspace/services/index.ts
export * from './workspace.service';
```

---

## 📝 步驟 4: 建立共用常數

### 4.1 響應式斷點 (`breakpoints.ts`)

```typescript
// src/app/shared/constants/breakpoints.ts

/**
 * 響應式斷點定義
 */
export const BREAKPOINTS = {
  // 自定義斷點
  mobile: '(max-width: 767px)',
  tablet: '(min-width: 768px) and (max-width: 1023px)',
  desktop: '(min-width: 1024px)',
  
  // Material CDK 斷點
  xs: '(max-width: 599px)',
  sm: '(min-width: 600px) and (max-width: 959px)',
  md: '(min-width: 960px) and (max-width: 1279px)',
  lg: '(min-width: 1280px) and (max-width: 1919px)',
  xl: '(min-width: 1920px)'
} as const;

/**
 * 斷點類型
 */
export type BreakpointName = keyof typeof BREAKPOINTS;
```

---

## ✅ 驗證清單

完成此階段後,確認以下項目:

- [ ] 所有領域實體定義檔案已建立
- [ ] AccountStore 可正常編譯
- [ ] WorkspaceStore 可正常編譯
- [ ] AccountService 可正常編譯
- [ ] WorkspaceService 可正常編譯
- [ ] 所有檔案都有正確的匯出和路徑別名 (@/domain, @/application, @/infrastructure)
- [ ] TypeScript 無編譯錯誤
- [ ] ESLint 無警告

---

## 🐛 常見問題

### Q: NgRx Signals 套件找不到?
**A**: 確認已安裝 `@ngrx/signals`:
```bash
yarn add @ngrx/signals
```

### Q: Firebase 類型錯誤?
**A**: 確認已安裝 `@angular/fire`:
```bash
yarn add @angular/fire
```

### Q: computed 函數無法使用?
**A**: 確認已從 `@angular/core` 匯入 `computed`

---

## 📚 相關資源

- [NgRx Signals 官方文件](https://ngrx.io/guide/signals)
- [Angular Fire 文件](https://github.com/angular/angularfire)
- [TypeScript 手冊](https://www.typescriptlang.org/docs/handbook/intro.html)

---

## ⏭️ 下一步

完成此階段後,請繼續 **階段 2: 身份切換器 - 基礎組件** (`02-身份切換器-基礎組件.md`)
